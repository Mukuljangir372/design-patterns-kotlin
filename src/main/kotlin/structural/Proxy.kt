package structural

/**
 * Proxy pattern is a structural pattern which is used to structure the
 * object by using proxy.
 */

private interface File
private class NormalFile : File
private class SecuredFile : File